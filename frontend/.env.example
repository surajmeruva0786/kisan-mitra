# Base URL of the deployed backend API, e.g. https://api.kisanmitra.example.com
# Leave unset for local dev — the Vite dev server proxies /api/* to http://localhost:4000
# (see vite.config.js), so no env var is needed there.
VITE_API_BASE_URL=
